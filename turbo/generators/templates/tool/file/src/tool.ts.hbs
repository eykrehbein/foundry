import { makeFunction } from "@usefoundry/utils";
import { z } from "zod";

export class {{pascalCase name}}Tool {
    constructor() {}

    public myFunction = makeFunction(
        z
            .object({
                name: z.string(),
            })
            .describe(
                "Does something important"
            ),
        async ({ name }) => {
            return name;
        }
    );
}

export default {{pascalCase name}}Tool;
